===========
Concepts
===========

The standard use case for rfpipe is to read visibility data, prepare it, search it, and save transient radio source information. The search work flow shown below and drives the concepts behind the rfpipe design. (Note references to databases are specific to the realfast system at the VLA.)

.. image:: rf-pipeline.png

.. toctree::
   State
   Preferences
   Metadata
   Searching
   Candidates